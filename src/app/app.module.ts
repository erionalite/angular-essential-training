import { NgModule } from "@angular/core";
import { BrowserModule } from "@angular/platform-browser";
import {AppComponenet} from './app.componenet'
@NgModule(
    {
        imports: [
            BrowserModule
        ],
        declarations:[],
        bootstrap:[
            AppComponenet
        ]
    }
)
export class AppModule{}